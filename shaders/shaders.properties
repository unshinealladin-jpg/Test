# ESP Shader Pack - OptiFine 1.8.9
# Placement : .minecraft/shaderpacks/ESP-Shader/shaders/
#
# Colortex layout:
#   colortex0 = couleur de la scène (rendu principal)
#   colortex1 = masque ESP entités (r=joueur, g=hostile, b=neutre)
#   colortex2 = blur horizontal du masque (composite.fsh → composite1.fsh)

# Activation des render targets supplémentaires
# (colortex1 et colortex2 nécessaires pour le glow ESP)
