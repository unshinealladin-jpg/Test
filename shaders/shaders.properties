# ESP Shader Pack - OptiFine 1.8.9 - Through-Wall (architecture minimale)
#
# Colortex layout:
#   colortex0 = scène complète (terrain + entités teintées via near-plane trick)
#   colortex1 = masque ESP entités (r=joueur, g=hostile, b=neutre)
#
# Le terrain, l'eau, le ciel et la main sont rendus par les shaders par
# défaut d'OptiFine — on ne les surcharge plus (évite les bugs d'écran blanc).
