# ESP Shader Pack - OptiFine 1.8.9 - Through-Wall Edition
#
# Colortex layout:
#   colortex0 = scène principale (terrain + eau + main) — sans entités
#   colortex1 = données ESP entités (r=joueur, g=hostile, b=neutre, a=profondeur réelle)
#   colortex2 = profondeur du terrain (r = gl_FragCoord.z)
#   colortex3 = blur horizontal du masque (intermédiaire composite)
